

describe("Test", () => {
    beforeEach(() => {

    });

    test("Test Case 1", () => {
        const name = "foo";
        const lastName = "bar";
        expect(name+lastName).toBe("foobar");
    });
});
